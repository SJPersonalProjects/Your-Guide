package com.example.yourguide;

public interface ImageGenerator {
    /**
     * Generates random images and returns their IDs.
     * @return image resource id
     */
    int randomImageGenerator();
}
